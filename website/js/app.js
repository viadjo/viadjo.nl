/**
 * ViaDjo Website - Main Application
 * Initializes all modules on DOMContentLoaded
 */
document.addEventListener('DOMContentLoaded', () => {
    initHeader();
    initMobileMenu();
    initServiceTabs();
    initPortfolioFilter();
    initTestimonialSlider();
    initScrollAnimations();
    initContactForm();
    initParallax();
    initScrollToTop();
});
